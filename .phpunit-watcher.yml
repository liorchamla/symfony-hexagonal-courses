watch:
  directories:
    - src
    - tests
    - domain
    - config
    - templates
  fileMask: ['*.php', '*.yaml']
notifications:
  passingTests: false
  failingTests: false
phpunit:
  binaryPath: vendor/bin/pest
  arguments: '--stop-on-failure'
  timeout: 180
